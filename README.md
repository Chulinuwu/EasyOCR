### Lab 2 : Submission (3% group, 1% individual in MCV)
To submit your lab results, please show that Experiment with OCR on different images and analyze which parameters were adjusted.

Select an appropriate confidence threshold and specify its value.

Design and implement a simple method to compare the recognized license plate text against a reference database (e.g., a text, CSV file, database) and display whether it passes or fails. If using a database, describe how you would design it and which database to use?

(Optional) Explore and implement a Cloud API OCR, instead of EasyOCR

(Optional) If we develop a real-time license plate recognition device, how should the communication system be designed, and which parts should run on the edge and which on the cloud?

(Optional) Send the message “Licence plate no. XXX XXXX: PASS (or Fail)” through Line.

![alt text](image.png)